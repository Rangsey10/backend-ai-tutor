rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Development emulator rules only. Do not deploy these rules to production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
